apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "edc.fullname" . }}-test-connection"
  labels:
    {{- include "edc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "edc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
