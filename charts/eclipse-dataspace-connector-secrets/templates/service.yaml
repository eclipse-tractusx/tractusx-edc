apiVersion: v1
kind: Service
metadata:
  name: {{ include "eclipse-dataspace-connector-secrets.fullname" . }}
  labels:
    {{- include "eclipse-dataspace-connector-secrets.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eclipse-dataspace-connector-secrets.selectorLabels" . | nindent 4 }}
