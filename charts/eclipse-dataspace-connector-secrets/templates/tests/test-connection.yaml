apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eclipse-dataspace-connector-secrets.fullname" . }}-test-connection"
  labels:
    {{- include "eclipse-dataspace-connector-secrets.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eclipse-dataspace-connector-secrets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
