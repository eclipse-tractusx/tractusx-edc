apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eclipse-dataspace-connector.fullname" . }}-test-connection"
  labels:
    {{- include "eclipse-dataspace-connector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eclipse-dataspace-connector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
