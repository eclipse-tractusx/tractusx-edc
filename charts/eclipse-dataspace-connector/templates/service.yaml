apiVersion: v1
kind: Service
metadata:
  name: {{ include "eclipse-dataspace-connector.fullname" . }}
  labels:
    {{- include "eclipse-dataspace-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eclipse-dataspace-connector.selectorLabels" . | nindent 4 }}
