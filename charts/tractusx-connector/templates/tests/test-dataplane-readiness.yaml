apiVersion: v1
kind: Pod
metadata:
  name: "{{include "txdc.fullname" .}}test-dataplane-readiness"
  labels:
    {{- include "txdc.dataplane.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: curlimages/curl
      command: ['curl']
      args: ['{{ include "tdxc.dataplane.url.readiness" . }}']
  restartPolicy: Never
