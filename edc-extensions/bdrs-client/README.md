# BDRS Client Extension

## Overview
The  BPN-DID Resolution Service (BDRS) Client extension provides a client for the BDRS server, which maintains mappings
between Business Partner Numbers (BPNs) and Decentralized Identifiers (DIDs) in the Catena-X dataspace.
The client maintains a local cache of these mappings for efficient resolution.